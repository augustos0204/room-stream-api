<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>403 - Acesso Negado | RoomStream</title>
    <link rel="icon" type="image/svg+xml" href="/view/assets/media/favicon.svg">
    <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/view/assets/styles/main.css">
</head>
<body class="min-h-screen bg-gradient-to-br from-slate-950 via-yellow-950 to-orange-950 text-white font-mono flex items-center justify-center p-4">
    
    <div class="relative max-w-2xl w-full">
        <div class="glass-effect rounded-2xl p-8 md:p-12 shadow-2xl">
            
            <!-- Icon -->
            <div class="flex justify-center mb-8">
                <div class="relative animate-float">
                    <div class="w-32 h-32 bg-gradient-to-br from-yellow-500 to-orange-600 rounded-full flex items-center justify-center shadow-2xl">
                        <i data-lucide="shield-off" class="w-16 h-16 text-white"></i>
                    </div>
                    <div class="absolute -bottom-2 -right-2 w-12 h-12 bg-gradient-to-br from-red-500 to-orange-600 rounded-full flex items-center justify-center shadow-xl">
                        <i data-lucide="lock" class="w-6 h-6 text-white"></i>
                    </div>
                </div>
            </div>

            <!-- Error Code -->
            <div class="text-center mb-6">
                <h1 class="text-8xl md:text-9xl font-black mb-4" style="background: linear-gradient(135deg, #f59e0b 0%, #f97316 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;">403</h1>
                <h2 class="text-2xl md:text-3xl font-bold text-gray-200 mb-2">
                    Acesso Negado
                </h2>
                <p class="text-gray-400 text-lg">
                    Você não tem permissão para acessar este recurso.
                </p>
            </div>

            <!-- Details (only in Development Mode) -->
            <% if (typeof isDevelopment !== 'undefined' && isDevelopment) { %>
            <div class="bg-gray-900/50 rounded-lg p-6 mb-8 border border-yellow-700">
                <div class="flex items-start gap-3">
                    <i data-lucide="alert-circle" class="w-5 h-5 text-yellow-400 flex-shrink-0 mt-1"></i>
                    <div class="flex-1">
                        <div class="flex items-center gap-2 mb-2">
                            <h3 class="text-sm font-semibold text-yellow-400">Informações de Acesso (Dev Mode)</h3>
                            <span class="px-2 py-1 bg-yellow-500/20 text-yellow-400 text-xs font-mono rounded">DEV</span>
                        </div>
                        <div class="space-y-2 text-sm text-gray-400">
                            <div class="flex justify-between">
                                <span>Status:</span>
                                <span class="text-yellow-400 font-mono">403 Forbidden</span>
                            </div>
                            <div class="flex justify-between">
                                <span>Path:</span>
                                <span class="text-purple-400 font-mono break-all"><%= path %></span>
                            </div>
                            <div class="flex justify-between">
                                <span>Timestamp:</span>
                                <span class="text-gray-300 font-mono"><%= timestamp %></span>
                            </div>
                            <% if (typeof messages !== 'undefined' && messages.length > 0) { %>
                            <div class="mt-3 pt-3 border-t border-gray-700">
                                <span class="text-xs text-gray-400 block mb-2"><%= messages.length === 1 ? 'Mensagem:' : 'Mensagens:' %></span>
                                <div class="space-y-2">
                                    <% messages.forEach((msg, index) => { %>
                                    <div class="flex gap-2">
                                        <% if (messages.length > 1) { %>
                                        <span class="text-yellow-400 font-mono text-xs flex-shrink-0"><%= index + 1 %>.</span>
                                        <% } %>
                                        <pre class="text-gray-200 font-mono text-xs bg-gray-800 p-3 rounded overflow-x-auto flex-1 whitespace-pre-wrap break-words m-0"><%= msg %></pre>
                                    </div>
                                    <% }); %>
                                </div>
                            </div>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
            <% } %>

            <!-- Suggestions -->
            <div class="mb-8">
                <h3 class="text-lg font-semibold text-gray-200 mb-4 flex items-center gap-2">
                    <i data-lucide="key" class="w-5 h-5 text-yellow-400"></i>
                    Possíveis Soluções
                </h3>
                <ul class="space-y-2 text-gray-300">
                    <li class="flex items-start gap-2">
                        <i data-lucide="check-circle" class="w-4 h-4 text-green-400 flex-shrink-0 mt-1"></i>
                        <span>Verifique se você está autenticado</span>
                    </li>
                    <li class="flex items-start gap-2">
                        <i data-lucide="check-circle" class="w-4 h-4 text-green-400 flex-shrink-0 mt-1"></i>
                        <span>Confira se possui as permissões necessárias</span>
                    </li>
                    <li class="flex items-start gap-2">
                        <i data-lucide="check-circle" class="w-4 h-4 text-green-400 flex-shrink-0 mt-1"></i>
                        <span>Entre em contato com o administrador para solicitar acesso</span>
                    </li>
                </ul>
            </div>

            <!-- Action Buttons -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <a href="/view" 
                   class="flex items-center justify-center gap-2 bg-gradient-to-r from-yellow-600 to-orange-600 hover:from-yellow-700 hover:to-orange-700 text-white font-semibold py-3 px-6 rounded-lg transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl">
                    <i data-lucide="log-in" class="w-5 h-5"></i>
                    <span>Fazer Login</span>
                </a>
                
                <a href="/" 
                   class="flex items-center justify-center gap-2 bg-gradient-to-r from-purple-600 to-blue-600 hover:from-purple-700 hover:to-blue-700 text-white font-semibold py-3 px-6 rounded-lg transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl">
                    <i data-lucide="home" class="w-5 h-5"></i>
                    <span>Página Inicial</span>
                </a>
            </div>

            <!-- Footer -->
            <div class="mt-8 text-center text-sm text-gray-500">
                <p>RoomStream API • <%= new Date().getFullYear() %></p>
            </div>
        </div>
    </div>

    <script>
        lucide.createIcons();
    </script>
</body>
</html>
