/**
 * Common interfaces used across the application
 *
 * This module exports all common interfaces that are shared between modules.
 *
 * @example
 * ```typescript
 * import { AuthenticatedRequest, MessageResponse } from '@common/interfaces';
 * ```
 */

export * from './request.interface';
export * from './response.interface';
